#include <stdio.h>

/**
 * main - Entry point
 * puts - Another print command
 * Return: Always 0 (Success)
 */

int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
			return (0);
}
