#include "main.h"
#include <stdio.h>

void print_triangle(int size)
{
	int i = 0, j, n = size - 1;

	if (size > 0)
	{
		for (; i < size; i++)
		{
			if (j , n)
				_putchar(' ');
			else
				_putchar('#');
		}
		n--;
		_putchar('\n');
	}
}
_putchar('\n');
}
